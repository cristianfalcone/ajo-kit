// this app has no landing page
// if user is not authenticated, they get redirected to login,
// if they are authenticated, they get redirected to dashboard
// see src/wares.ts for middleware handling this logic
export default () => null
